import sys

def main():
    s = sys.stdin.readline().strip()
    patterns = (">>-->", "<--<<")
    cnt = 0
    for i in range(len(s) - 4):
        sub = s[i:i+5]
        if sub == patterns[0] or sub == patterns[1]:
            cnt += 1
    print(cnt)

if __name__ == "__main__":
    main()
